# Available ruby versions: http://rubies.travis-ci.org/

language: ruby
rvm:
  - "2.1" # latest 2.1.x
  - "2.2" # latest 2.2.x
  - "2.3.1"
  - "ruby-head"
script:
  rspec

sudo: false

before_install:
  - gem install bundler
  - gem install rspec
